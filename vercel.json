{
  "version": 2,
  "functions": {
    "paystack/api/index.js": {
      "runtime": "nodejs20.x"
    }
  },
  "installCommand": "npm ci --prefix paystack || npm install --prefix paystack",
  "buildCommand": "echo \"no build step\"",
  "routes": [
    { "src": "/(.*)", "dest": "paystack/api/index.js" }
  ]
}